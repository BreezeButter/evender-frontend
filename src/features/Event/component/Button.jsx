export default function Button({ width, height }) {
    return (
        <button
            className={`bg-black text-white rounded-full w-60 h-10 self-center`}
        >
            Create Event
        </button>
    );
}
